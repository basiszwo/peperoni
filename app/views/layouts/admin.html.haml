!!! Transitional
%html

  %head
    %title= "#{controller.controller_name}: #{controller.action_name} | Administration"
    
    %meta{ :content => "text/html; charset=UTF-8", "http-equiv" => "content-type" }/
    %meta{ :content => "false", "http-equiv" => "imagetoolbar" }/
    %link{ "rel" => "shortcut icon", "type" => "image/x-icon",  "href" => "/favicon.ico" }/
    
    = stylesheet_link_tag 'admin', :media => 'screen, projection'

  %body
    #container
      
      #header        
        = render :partial => 'admin/layouts/login_box'
      
      #content_outer
        #flash_messages
          - flash.each do |key, value|
            %div{:class => key}= h(value)

        #content
          = yield
      
      = render :partial => 'admin/layouts/footer'
