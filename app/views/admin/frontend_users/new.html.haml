%h1 Register new FrontendUser

- form_for @frontend_user, :url => admin_frontend_users_path do |form|
  = render :partial => 'form', :locals => {:form => form}
  
  %p.button
    = form.submit "Create"
    or
    = link_to "Back", admin_frontend_users_path
